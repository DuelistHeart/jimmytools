# Startup Commands

Run a command automatically when you join the server. Currently only `/names` is supported.

Configure it under *Config → ChatUtils*:

1. Turn on **Enable Names command**.
2. Pick which command to run: `/names on`, `/names off` or `/names character`.

The command only runs when joining `lords.rawb.tv`.
